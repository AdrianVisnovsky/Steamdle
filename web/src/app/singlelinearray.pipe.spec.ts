import { SinglelinearrayPipe } from './singlelinearray.pipe';

describe('SinglelinearrayPipe', () => {
  it('create an instance', () => {
    const pipe = new SinglelinearrayPipe();
    expect(pipe).toBeTruthy();
  });
});
