import { MultilineArrayPipe } from './multilinearray.pipe';

describe('MultilineArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new MultilineArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
